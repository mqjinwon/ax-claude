# AX Study v2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `/ax-study` 스킬에 자연어 자동 모드 감지, 응답 하단 힌트 시스템, Active Recall Quiz 모드, Feynman 멀티턴 모드를 추가한다.

**Architecture:** SKILL.md 단일 파일에 모든 변경 사항을 적용한다. Mode Detection을 우선순위 기반 자연어 분류로 개선하고, 모든 응답 하단에 컨텍스트 맞춤 힌트를 고정한다. Quiz/Feynman 모드는 NLM 연결 여부에 따라 하이브리드로 동작한다.

**Tech Stack:** Bash, YAML, Markdown (SKILL.md는 Claude가 실행하는 프롬프트 문서)

---

## File Map

| 파일 | 변경 유형 | 역할 |
|---|---|---|
| `skills/ax-study/SKILL.md` | 수정 | description 필드, Mode Detection, Hint Footer, Quiz/Feynman 모드 |
| `routing/skill-routing.yaml` | 수정 | study 카테고리에 examples 추가 (TF-IDF용) |
| `templates/STUDY_NOTES.template.md` | 수정 | mastery / next-review 섹션 추가 |
| `VERSION` | 수정 | 1.18.0 |
| `.claude-plugin/plugin.json` | 수정 | 1.18.0 |
| `.claude-plugin/marketplace.json` | 수정 | 1.18.0 (2곳) |

---

## Task 1: skill-routing.yaml — study examples 추가

**Files:**
- Modify: `routing/skill-routing.yaml:363-381`

- [ ] **Step 1: 현재 study 카테고리 확인**

```bash
grep -n -A 20 "^  study:" routing/skill-routing.yaml
```

Expected output:
```
363:  study:
364:    canonical: ax-study
...
380:    orchestrator_trigger: []
```

- [ ] **Step 2: examples 필드 추가**

`routing/skill-routing.yaml`의 study 카테고리에서 `orchestrator_trigger: []` 줄을 다음으로 교체한다:

```yaml
    orchestrator_trigger: []
    examples:
      - "이 논문 읽고 핵심 개념 정리해줘"
      - "이 개념이 이해가 잘 안 가"
      - "공부한 내용 퀴즈로 테스트해줘"
      - "Transformer 구조를 설명해야 할 것 같아"
      - "이 문서 학습 시작할게"
```

- [ ] **Step 3: YAML 구문 검증**

```bash
python3 -c "import yaml; yaml.safe_load(open('routing/skill-routing.yaml'))" && echo "OK"
```

Expected: `OK`

- [ ] **Step 4: commit**

```bash
git add routing/skill-routing.yaml
git commit -m "feat(routing): add study category examples for TF-IDF auto-detection"
```

---

## Task 2: STUDY_NOTES.template.md — mastery/next-review 섹션 추가

**Files:**
- Modify: `templates/STUDY_NOTES.template.md`

- [ ] **Step 1: 현재 파일 확인**

```bash
cat -n templates/STUDY_NOTES.template.md
```

Expected: 23줄, Open Questions 섹션이 마지막.

- [ ] **Step 2: mastery + next-review 섹션 추가**

파일 끝에 두 섹션을 추가한다. 파일 내용 전체:

```markdown
# Study Notes — {{project_slug}}

> Managed by /ax-study. Edit content inside sections freely; do not modify the delimiter comments.

## Active Document
<!-- BEGIN:active-document -->
_No document loaded yet._
<!-- END:active-document -->

## Study Queue
<!-- BEGIN:study-queue -->
_No study items queued._
<!-- END:study-queue -->

## Concept Notes
<!-- BEGIN:concept-notes -->
_No concepts recorded yet._
<!-- END:concept-notes -->

## Open Questions
<!-- BEGIN:open-questions -->
_No open questions yet._
<!-- END:open-questions -->

## Mastery
<!-- BEGIN:mastery -->
_No mastery data yet._
<!-- END:mastery -->

## Next Review
<!-- BEGIN:next-review -->
_No review scheduled._
<!-- END:next-review -->
```

- [ ] **Step 3: 섹션 존재 검증**

```bash
grep -c "BEGIN:mastery\|BEGIN:next-review" templates/STUDY_NOTES.template.md
```

Expected: `2`

- [ ] **Step 4: commit**

```bash
git add templates/STUDY_NOTES.template.md
git commit -m "feat(template): add mastery and next-review sections to study-notes"
```

---

## Task 3: SKILL.md — description 필드 + Mode Auto-Detect 개선

**Files:**
- Modify: `skills/ax-study/SKILL.md:1-16` (frontmatter)
- Modify: `skills/ax-study/SKILL.md:65-73` (Mode Detection 테이블)

- [ ] **Step 1: 현재 frontmatter 확인**

```bash
head -16 skills/ax-study/SKILL.md
```

- [ ] **Step 2: frontmatter description 필드 교체**

`skills/ax-study/SKILL.md` 파일의 frontmatter(`---`로 둘러싸인 부분)를 다음으로 교체한다:

```yaml
---
name: ax-study
version: 1.18.0
description: |
  /ax-study <pdf|url>          문서/논문 학습 시작
  /ax-study quiz [N]           배운 내용 퀴즈 (Active Recall, 기본 5문제)
  /ax-study feynman <개념>     이해도 검증 (멀티턴 소크라테스 대화)
  /ax-study concept <개념>     개념 심화 학습
  /ax-study explore <주제>     후속 자료 탐색
  /ax-study audio              오디오 요약 생성
  또는 자연어: "이해 안가" → feynman 자동, "퀴즈 내줘" → quiz 자동
allowed-tools:
  - Bash
  - Read
  - Edit
  - Write
---
```

- [ ] **Step 3: Mode Detection 테이블 교체**

현재 Mode Detection 섹션(`## Mode Detection`부터 테이블 끝까지)을 다음으로 교체한다:

```markdown
## Mode Detection

Determine mode from the user's input AFTER the `/ax-study` command.
Apply rules in priority order — first match wins.

| 우선순위 | 입력 패턴 | 모드 |
|---|---|---|
| 1 | URL(`https?://`) 또는 파일 경로(`.pdf`, `.md`, `~/`, `/home/`) | Init Mode |
| 2 | `quiz` 또는 `quiz <숫자>` | Quiz Mode |
| 3 | `feynman <개념명>` | Feynman Mode |
| 4 | `concept <name>` | Concept Mode |
| 5 | `explore <topic>` | Explore Mode |
| 6 | `audio` | Audio Mode |
| 7 | 자연어 퀴즈 의도: "테스트", "퀴즈", "문제 내줘", "맞혀볼게", "확인해줘", "시험", "test me", "quiz me", "check my understanding" | Quiz Mode |
| 8 | 자연어 Feynman 의도: "이해가 안 가", "모르겠어", "헷갈려", "다시 설명", "쉽게", "어렵다", "confused", "don't understand", "explain to me" + (개념명 있으면 바로 feynman, 없으면 개념명 질문 후 feynman) | Feynman Mode |
| 9 | 자연어 개념: "<명사> 뭐야", "<명사> 알려줘", "<명사> 설명해줘" → concept <명사> 로 변환 | Concept Mode |
| 10 | (아무것도 없음) | Resume Mode |
```

- [ ] **Step 4: 변경 검증**

```bash
grep -c "Quiz Mode\|Feynman Mode" skills/ax-study/SKILL.md
```

Expected: `4` 이상 (테이블 행 포함)

```bash
grep "version:" skills/ax-study/SKILL.md
```

Expected: `version: 1.18.0`

- [ ] **Step 5: commit**

```bash
git add skills/ax-study/SKILL.md
git commit -m "feat(study): improve Mode Auto-Detect with natural language intent classification"
```

---

## Task 4: SKILL.md — Hint Footer 시스템 추가

**Files:**
- Modify: `skills/ax-study/SKILL.md` — 각 모드 출력 형식에 힌트 섹션 추가

이 태스크는 두 단계로 나뉜다:
1. Hint Footer 템플릿 섹션 신규 추가 (파일 끝)
2. 기존 모드 출력 형식(`Output format`)에 힌트 호출 추가

- [ ] **Step 1: Hint Footer 섹션을 SKILL.md 끝에 추가**

`## Error Reference` 섹션 직전에 다음 섹션을 삽입한다:

```markdown
---

## Hint Footer

모든 모드의 응답 마지막에 아래 힌트를 출력한다. active-document 존재 여부에 따라 분기한다.

### active-document 있을 때 (기본)

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
다음 액션:
  /ax-study quiz               ← 배운 내용 테스트
  /ax-study feynman <개념>     ← 이해도 검증
  /ax-study concept <개념>     ← 개념 심화
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### active-document 없을 때 (초기)

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
시작하려면:
  /ax-study <pdf 경로>         ← 로컬 PDF
  /ax-study <url>              ← 웹 문서 / 논문
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### Quiz 완료 후

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
결과: {정답}/{총} 정답 | 약점: [{약점 개념 목록}]
다음:
  /ax-study feynman <약점 개념>  ← 약점 집중
  /ax-study concept <약점 개념>  ← 개념 재학습
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

약점 없으면 (전체 정답):
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🎉 전체 정답! 다음 단계:
  /ax-study explore <주제>     ← 후속 탐색
  /ax-study audio              ← 오디오 요약
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### Feynman 완료 후

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ Feynman 통과: {개념명} | 숙달 상태: feynman-passed
다음:
  /ax-study quiz               ← 전체 퀴즈로 확인
  /ax-study explore <주제>     ← 후속 탐색
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

Feynman 중단 시:
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
약점 기록: {개념명} → weak
다음:
  /ax-study concept {개념명}   ← 개념 재학습
  /ax-study feynman {개념명}   ← 다시 도전
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```
```

- [ ] **Step 2: Resume Mode 출력 형식에 힌트 추가**

Resume Mode의 `Output format` 끝에 다음을 추가한다:

```
[active-document 있을 때 → Hint Footer: active-document 있을 때]
[active-document 없을 때 → Hint Footer: active-document 없을 때]
```

- [ ] **Step 3: Init Mode 출력 형식에 힌트 추가**

Init Mode의 `Output format` 끝에 추가:

```
[Hint Footer: active-document 있을 때]
```

- [ ] **Step 4: Concept Mode 출력 형식에 힌트 추가**

Concept Mode의 `Output format` 끝에 추가:

```
[Hint Footer: active-document 있을 때]
```

- [ ] **Step 5: Hint Footer 섹션 존재 검증**

```bash
grep -c "Hint Footer\|━━━" skills/ax-study/SKILL.md
```

Expected: `8` 이상

- [ ] **Step 6: commit**

```bash
git add skills/ax-study/SKILL.md
git commit -m "feat(study): add contextual hint footer to all modes"
```

---

## Task 5: SKILL.md — Quiz 모드 추가

**Files:**
- Modify: `skills/ax-study/SKILL.md` — Feynman 모드 섹션 직전에 Quiz 모드 삽입

- [ ] **Step 1: Quiz 모드 섹션 삽입 위치 확인**

```bash
grep -n "## Audio Mode\|## Explore Mode\|## Error Reference" skills/ax-study/SKILL.md
```

Expected: Audio Mode와 Error Reference 사이의 빈 공간을 찾는다.

- [ ] **Step 2: Quiz 모드 섹션 추가**

`## Audio Mode` 섹션과 `## Error Reference` 섹션 사이에 다음을 삽입한다:

````markdown
---

## Quiz Mode

**Trigger**: `/ax-study quiz [N]` 또는 자연어 퀴즈 의도 (Mode Detection 참고)

N 미지정 시 기본 5문제. `quiz 10`처럼 숫자 지정 가능.

### Step 1: 컨텍스트 로드

```bash
PLUGIN_ROOT="${CLAUDE_PLUGIN_ROOT:-}"
if [ -z "$PLUGIN_ROOT" ] || [ ! -f "$PLUGIN_ROOT/lib/ax-utils.sh" ]; then
  for _P in \
    $(ls -d "$HOME/.claude/plugins/cache/ax-claude/ax-claude/"* 2>/dev/null | sort -V -r | head -1) \
    "$HOME/.claude/plugins/marketplaces/ax-claude" \
    "$HOME/.ax"; do
    [ -f "$_P/lib/ax-utils.sh" ] && PLUGIN_ROOT="$_P" && break
  done
fi
PLUGIN_ROOT="${PLUGIN_ROOT:-$HOME/.ax}"
source "$PLUGIN_ROOT/lib/ax-utils.sh"
PROJECT_ROOT=$(git rev-parse --show-toplevel 2>/dev/null || pwd)
STUDY_NOTES="$PROJECT_ROOT/.ax/memory/study-notes.md"

echo "=== ACTIVE DOCUMENT ==="
ax_get_section "$STUDY_NOTES" "active-document"
echo "=== CONCEPT NOTES ==="
ax_get_section "$STUDY_NOTES" "concept-notes"
```

active-document가 비어 있으면: "No active study. Run `/ax-study <pdf-path-or-url>` to start." 출력 후 Hint Footer(초기) 표시하고 종료.

> **기존 프로젝트 호환:** mastery/next-review 섹션이 study-notes.md에 없으면 `ax_replace_section`이 새 섹션으로 삽입한다 (ax-utils.sh 표준 동작).

### Step 2: 문제 생성 (하이브리드)

**N** = 사용자가 지정한 숫자 또는 기본값 5.

**NLM 경로 (연결 있을 때):**

notebook_id를 active-document에서 읽어 `notebook_query` MCP 호출:

```
query: "Generate {N} Q&A pairs covering the key concepts of this document.
Format each pair exactly as:
Q: [question in Korean or English matching the document language]
A: [concise answer, 1-3 sentences]

Focus on concepts that require deep understanding, not simple facts."
```

**Claude fallback (NLM 없거나 실패 시):**

`concept-notes` 섹션의 각 `### {Concept Name}` 항목에서 Q&A 생성:
- Q: Definition/Key insight 기반 질문
- A: Definition + Key insight 결합 답변

최대 N개 생성. concept-notes 항목이 N보다 적으면 가능한 만큼만.

### Step 3: 퀴즈 세션 진행

문제를 한 번에 하나씩 제시. 사용자 답변 후 다음 문제로 이동.

**출력 형식 (문제별):**

```
**Q{번호}/{총}: {질문}**

> (답변을 입력하세요)
```

**사용자 답변 후 평가:**

```
✅ 정답 — {한 줄 피드백}
```
또는
```
⚠️ 부분 정답 — 빠진 점: {설명}
   정답: {핵심 답변}
```
또는
```
❌ 틀림 — 정답: {핵심 답변}
   핵심 포인트: {1-2줄 설명}
```

세션 중 `그만` / `skip` / `stop` 입력 시 즉시 종료 후 Step 4로.

### Step 4: study-notes.md 업데이트

```bash
PLUGIN_ROOT="${CLAUDE_PLUGIN_ROOT:-}"
if [ -z "$PLUGIN_ROOT" ] || [ ! -f "$PLUGIN_ROOT/lib/ax-utils.sh" ]; then
  for _P in \
    $(ls -d "$HOME/.claude/plugins/cache/ax-claude/ax-claude/"* 2>/dev/null | sort -V -r | head -1) \
    "$HOME/.claude/plugins/marketplaces/ax-claude" \
    "$HOME/.ax"; do
    [ -f "$_P/lib/ax-utils.sh" ] && PLUGIN_ROOT="$_P" && break
  done
fi
PLUGIN_ROOT="${PLUGIN_ROOT:-$HOME/.ax}"
source "$PLUGIN_ROOT/lib/ax-utils.sh"
PROJECT_ROOT=$(git rev-parse --show-toplevel 2>/dev/null || pwd)
STUDY_NOTES="$PROJECT_ROOT/.ax/memory/study-notes.md"
DATE=$(date +%Y-%m-%d)
REVIEW_DATE=$(date -d "+3 days" +%Y-%m-%d 2>/dev/null || date -v+3d +%Y-%m-%d 2>/dev/null || echo "unknown")

# mastery 섹션 업데이트 (기존 항목 유지, 신규/변경만 반영)
MASTERY_FILE=$(mktemp)
ax_get_section "$STUDY_NOTES" "mastery" \
  | grep -v '^_No mastery data yet' > "$MASTERY_FILE" || true

# <INSERT_MASTERY_LINES> — 평가 결과에 따라 아래 형식으로 추가/갱신:
# ✅ 정답 개념: "- {concept}: mastered"
# ⚠️ 부분 정답: "- {concept}: learning"
# ❌ 틀림: "- {concept}: weak"

ax_replace_section "$STUDY_NOTES" "mastery" "$MASTERY_FILE"
rm -f "$MASTERY_FILE"

# next-review 섹션 업데이트 (약점 있을 때만)
if [ -n "<INSERT_WEAK_CONCEPTS>" ]; then
  REVIEW_FILE=$(mktemp)
  ax_get_section "$STUDY_NOTES" "next-review" \
    | grep -v '^_No review scheduled' > "$REVIEW_FILE" || true
  printf '- %s: %s\n' "$REVIEW_DATE" "<INSERT_WEAK_CONCEPTS>" >> "$REVIEW_FILE"
  ax_replace_section "$STUDY_NOTES" "next-review" "$REVIEW_FILE"
  rm -f "$REVIEW_FILE"
fi
```

`<INSERT_MASTERY_LINES>` 와 `<INSERT_WEAK_CONCEPTS>` 는 실제 quiz 결과값으로 교체한 후 실행한다.

### Output format

```
=== AX Study: Quiz 완료 ===

결과: {정답}/{총} 정답
{정답 개념}: ✅ {개념명} ...
{틀린 개념}: ❌ {개념명} ...

study-notes.md mastery 업데이트 완료.
```

[Quiz 완료 힌트 — Hint Footer: Quiz 완료 후 참고]

---
````

- [ ] **Step 3: Quiz 모드 섹션 존재 검증**

```bash
grep -c "## Quiz Mode\|Step 1: 컨텍스트 로드\|하이브리드\|study-notes.md 업데이트" skills/ax-study/SKILL.md
```

Expected: `4`

- [ ] **Step 4: commit**

```bash
git add skills/ax-study/SKILL.md
git commit -m "feat(study): add Quiz mode with NLM/Claude hybrid Q&A and mastery tracking"
```

---

## Task 6: SKILL.md — Feynman 모드 추가

**Files:**
- Modify: `skills/ax-study/SKILL.md` — Quiz 모드와 Audio 모드 사이에 삽입

- [ ] **Step 1: 삽입 위치 확인**

```bash
grep -n "## Audio Mode\|## Quiz Mode" skills/ax-study/SKILL.md
```

Feynman 섹션은 `## Quiz Mode` 끝과 `## Audio Mode` 사이에 삽입한다.

- [ ] **Step 2: Feynman 모드 섹션 추가**

````markdown
---

## Feynman Mode

**Trigger**: `/ax-study feynman <개념명>` 또는 자연어 Feynman 의도 (Mode Detection 참고)

개념명이 없으면 먼저 묻는다: "어떤 개념이 어려우신가요?"

최대 5라운드. 갭이 없으면 조기 완료.

### Step 1: 컨텍스트 로드 및 개념명 확인

```bash
PLUGIN_ROOT="${CLAUDE_PLUGIN_ROOT:-}"
if [ -z "$PLUGIN_ROOT" ] || [ ! -f "$PLUGIN_ROOT/lib/ax-utils.sh" ]; then
  for _P in \
    $(ls -d "$HOME/.claude/plugins/cache/ax-claude/ax-claude/"* 2>/dev/null | sort -V -r | head -1) \
    "$HOME/.claude/plugins/marketplaces/ax-claude" \
    "$HOME/.ax"; do
    [ -f "$_P/lib/ax-utils.sh" ] && PLUGIN_ROOT="$_P" && break
  done
fi
PLUGIN_ROOT="${PLUGIN_ROOT:-$HOME/.ax}"
source "$PLUGIN_ROOT/lib/ax-utils.sh"
PROJECT_ROOT=$(git rev-parse --show-toplevel 2>/dev/null || pwd)
STUDY_NOTES="$PROJECT_ROOT/.ax/memory/study-notes.md"

ax_get_section "$STUDY_NOTES" "active-document" | grep "Notebook ID"
ax_get_section "$STUDY_NOTES" "concept-notes" | grep -A 5 "{CONCEPT_NAME}" || true
```

active-document 없으면: "No active study." 출력 후 Hint Footer(초기) 표시하고 종료.

### Step 2: 라운드 1 — 설명 요청

다음 메시지 출력 후 사용자 답변 대기:

```
=== AX Study: Feynman — {개념명} ===

'{개념명}'을 초등학생에게 설명한다고 가정하고 설명해보세요. [1/5]
```

### Step 3: 갭 탐지 (하이브리드)

**NLM 경로 (연결 있을 때):**

```
notebook_query:
  "A student explained '{concept}' as follows: '{user_explanation}'
   Based on this document, what key points are missing or incorrect?
   List each gap as a single question I can ask the student to guide them.
   If the explanation is complete and correct, respond with: COMPLETE"
```

**Claude fallback (NLM 없거나 실패 시):**

`concept-notes`의 해당 개념 항목(`Definition`, `Key insight`, `Related` 필드)을 기준으로 사용자 설명과 비교하여 갭 판단.

### Step 4: 라운드 2~5 — 소크라테스 질문 또는 완료

**갭 없음 (COMPLETE 또는 Claude 판단):**

→ 완료 처리 (Step 5-A)

**갭 있음:**

```
[{현재라운드}/{최대라운드}] {갭 기반 소크라테스 질문}
```

사용자 답변 대기 → 다시 Step 3으로.

**사용자가 `포기` / `skip` / `그만` 입력:**

→ 중단 처리 (Step 5-B)

### Step 5-A: 완료 처리 및 study-notes.md 업데이트

```bash
PLUGIN_ROOT="${CLAUDE_PLUGIN_ROOT:-}"
if [ -z "$PLUGIN_ROOT" ] || [ ! -f "$PLUGIN_ROOT/lib/ax-utils.sh" ]; then
  for _P in \
    $(ls -d "$HOME/.claude/plugins/cache/ax-claude/ax-claude/"* 2>/dev/null | sort -V -r | head -1) \
    "$HOME/.claude/plugins/marketplaces/ax-claude" \
    "$HOME/.ax"; do
    [ -f "$_P/lib/ax-utils.sh" ] && PLUGIN_ROOT="$_P" && break
  done
fi
PLUGIN_ROOT="${PLUGIN_ROOT:-$HOME/.ax}"
source "$PLUGIN_ROOT/lib/ax-utils.sh"
PROJECT_ROOT=$(git rev-parse --show-toplevel 2>/dev/null || pwd)
STUDY_NOTES="$PROJECT_ROOT/.ax/memory/study-notes.md"
DATE=$(date +%Y%m%d)

# mastery 업데이트: feynman-passed
MASTERY_FILE=$(mktemp)
ax_get_section "$STUDY_NOTES" "mastery" \
  | grep -v "^- <INSERT_CONCEPT_NAME>:" \
  | grep -v '^_No mastery data yet' > "$MASTERY_FILE" || true
printf '- %s: feynman-passed\n' "<INSERT_CONCEPT_NAME>" >> "$MASTERY_FILE"
ax_replace_section "$STUDY_NOTES" "mastery" "$MASTERY_FILE"
rm -f "$MASTERY_FILE"

# concept-notes 해당 항목에 feynman 날짜 기록
# concept-notes 섹션을 읽어 해당 개념의 entry에 "- **Feynman**: YYYYMMDD" 추가
NOTES_FILE=$(mktemp)
ax_get_section "$STUDY_NOTES" "concept-notes" > "$NOTES_FILE" || true
# <INSERT_CONCEPT_NAME>을 실제 개념명으로, DATE를 실제 날짜로 교체 후 실행
ax_replace_section "$STUDY_NOTES" "concept-notes" "$NOTES_FILE"
rm -f "$NOTES_FILE"
```

`<INSERT_CONCEPT_NAME>` 은 실제 개념명으로 교체 후 실행한다.

### Step 5-B: 중단 처리 및 study-notes.md 업데이트

```bash
# Step 5-A와 동일한 bash 환경 설정 후:

# mastery 업데이트: weak
MASTERY_FILE=$(mktemp)
ax_get_section "$STUDY_NOTES" "mastery" \
  | grep -v "^- <INSERT_CONCEPT_NAME>:" \
  | grep -v '^_No mastery data yet' > "$MASTERY_FILE" || true
printf '- %s: weak\n' "<INSERT_CONCEPT_NAME>" >> "$MASTERY_FILE"
ax_replace_section "$STUDY_NOTES" "mastery" "$MASTERY_FILE"
rm -f "$MASTERY_FILE"
```

### Output format — 완료

```
=== AX Study: Feynman — {개념명} ===

✅ Feynman 검증 통과! ({N}라운드 완료)

핵심 인사이트:
- {핵심 포인트 1}
- {핵심 포인트 2}

study-notes.md: {개념명} → feynman-passed
```

[Hint Footer: Feynman 완료 후]

### Output format — 중단

```
=== AX Study: Feynman — {개념명} (중단) ===

발견된 갭:
- {미완성 포인트 목록}

study-notes.md: {개념명} → weak
```

[Hint Footer: Feynman 중단 시]

---
````

- [ ] **Step 3: Feynman 모드 섹션 존재 검증**

```bash
grep -c "## Feynman Mode\|소크라테스\|COMPLETE\|feynman-passed" skills/ax-study/SKILL.md
```

Expected: `4`

- [ ] **Step 4: commit**

```bash
git add skills/ax-study/SKILL.md
git commit -m "feat(study): add Feynman multi-turn mode with NLM/Claude hybrid gap detection"
```

---

## Task 7: VERSION 1.18.0 + 전체 구조 검증

**Files:**
- Modify: `VERSION`
- Modify: `.claude-plugin/plugin.json`
- Modify: `.claude-plugin/marketplace.json`

- [ ] **Step 1: 현재 버전 확인**

```bash
cat VERSION
python3 -c "import json; d=json.load(open('.claude-plugin/plugin.json')); print(d['version'])"
python3 -c "import json; d=json.load(open('.claude-plugin/marketplace.json')); print(d['version'], d['plugins'][0]['version'])"
```

Expected: `1.17.0` (세 곳 모두)

- [ ] **Step 2: VERSION 파일 업데이트**

`VERSION` 파일 내용을 `1.18.0`으로 변경.

- [ ] **Step 3: plugin.json 업데이트**

`.claude-plugin/plugin.json`의 `"version"` 필드를 `"1.18.0"`으로 변경.

- [ ] **Step 4: marketplace.json 업데이트**

`.claude-plugin/marketplace.json`의 두 `"version"` 필드(최상위 + `plugins[0].version`)를 모두 `"1.18.0"`으로 변경.

- [ ] **Step 5: 버전 동기화 검증**

```bash
echo "VERSION: $(cat VERSION)"
python3 -c "import json; d=json.load(open('.claude-plugin/plugin.json')); print('plugin.json:', d['version'])"
python3 -c "import json; d=json.load(open('.claude-plugin/marketplace.json')); print('marketplace.json top:', d['version']); print('marketplace.json plugins[0]:', d['plugins'][0]['version'])"
```

Expected: 모두 `1.18.0`

- [ ] **Step 6: SKILL.md 전체 구조 검증**

```bash
echo "=== 섹션 목록 ==="
grep "^## " skills/ax-study/SKILL.md

echo "=== 핵심 키워드 ==="
grep -c "Quiz Mode\|Feynman Mode\|Hint Footer\|Mode Auto-Detect\|mastery\|next-review" skills/ax-study/SKILL.md

echo "=== YAML 검증 ==="
python3 -c "import yaml; yaml.safe_load(open('routing/skill-routing.yaml'))" && echo "routing YAML OK"

echo "=== 템플릿 검증 ==="
grep -c "BEGIN:mastery\|BEGIN:next-review" templates/STUDY_NOTES.template.md
```

Expected:
- 섹션 목록에 `## Quiz Mode`, `## Feynman Mode`, `## Hint Footer` 포함
- 핵심 키워드 grep: `6` 이상
- `routing YAML OK`
- 템플릿 grep: `2`

- [ ] **Step 7: commit**

```bash
git add VERSION .claude-plugin/plugin.json .claude-plugin/marketplace.json
git commit -m "chore: bump version to 1.18.0"
```

---

## 성공 기준 체크리스트

구현 완료 후 다음을 수동으로 확인한다:

- [ ] `"transformer 이해 안가"` 입력 → Mode Detection이 feynman으로 분류
- [ ] `"퀴즈 내줘"` 입력 → Mode Detection이 quiz로 분류
- [ ] `/ax-study quiz` → NLM 없어도 concept-notes 기반 문제 생성
- [ ] Feynman 5라운드 후 자동 종료 또는 갭 없으면 조기 완료
- [ ] 모든 모드 응답 하단에 `━━━` 힌트 블록 표시
- [ ] study-notes.md에 mastery/next-review 섹션 존재
- [ ] VERSION 1.18.0, 3파일 동기화
